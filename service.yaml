apiVersion: v1
kind: Service
metadata:
  name: springboot-api-service
spec:
  type: NodePort
  selector:
    app: springboot-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30036
